//Install react router library via npm install --save react-router-dom@6

import LoginForm from "../components/LoginForm";

export default function LoginPage() {
  return (
    <div>
        <h1>Login</h1>
        <LoginForm/>
      
    </div>
  )
}
